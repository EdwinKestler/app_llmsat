# Core
streamlit>=1.30
altair>=5.0
pandas>=2.0
geopandas>=0.14
numpy>=1.26
rasterio>=1.3
shapely>=2.0
Pillow>=10.0

# Interactive map
folium>=0.15
streamlit-folium>=0.20

# NL query & vision analysis
openai>=1.0
python-dotenv>=1.0

# Config
pyyaml>=6.0

# Segmentation (SAM3)
segment-geospatial[samgeo2,samgeo3]>=1.2
transformers>=5.1  # RF-DETR requires >=5.1; GroundingDINO may need samgeo update
psutil>=5.9

# Open Buildings spatial queries
duckdb>=0.9

# System (install via apt: libgdal-dev gdal-bin)
# GDAL  # version must match system libgdal

# Object detection — primary (RF-DETR transformer, GPU)
rfdetr>=1.4
supervision>=0.26.1

# Object detection — legacy CPU fallback (GeoDeep ONNX models)
geodeep>=0.9.12

# Visualization
matplotlib>=3.7
